# Contributing to LumiCore

感谢你参与 LumiCore。项目优先接受能够提升稳定性、隐私边界、跨平台一致性、可验证执行和行业工作流复用度的改动。

## 开始之前

1. 在 Issue 中说明问题、使用场景和可验证结果；小型修复可直接提交 Pull Request。
2. 从最新 `main` 创建短生命周期分支。
3. 不要提交真实 API Key、访问令牌、客户文件、个人数据库、聊天记录、语音样本、设备身份或本机绝对路径配置。
4. 新增外部副作用必须进入统一意图、确认、幂等和回执管线，不能增加绕过策略的快捷分支。

## 本地验证

```bash
npm ci
npm run lint
npm test -- --run
npm run build:frontends
npm run security:audit
```

涉及桌面壳、安装包或运行时资源时，还应按平台运行对应的构建和冒烟门禁。测试结果应写入 Pull Request，不要只描述“应该可以”。

## 提交要求

- 一个提交尽量只解决一个可解释的问题。
- 使用清晰的英文 Conventional Commit 前缀，例如 `fix:`, `feat:`, `test:`, `docs:`。
- 行为变化必须补测试；UI 变化应附前后截图和窗口尺寸。
- 生成内容、构建产物、日志和本地运行数据不得进入 Git。
- 修改模型、语音或工具适配时，说明失败、超时、结果未知和降级策略。

## 通用能力与行业版本

主仓库只接收可跨客户复用的核心能力。客户名称、私有资料、专用账号和不可复用流程应保留在对应定制仓库；可复用能力通过独立提交和 Pull Request 反哺主线。

提交代码即表示你有权贡献该内容，并同意按本仓库的 AGPL-3.0 许可证发布。
