# Lumi 核心能力稳定化与智能编排实施账本

## 基线

- 唯一需求源：`C:\Users\Administrator\.codex\attachments\4e2ced60-2a54-4203-86ed-812d4a89203a\pasted-text.txt`
- 需求源 SHA-256：`FD06C4735AFFABCD2590BCD6CFDB340AF6F65FCCD02CB369CB7DFD07695C708D`
- 实施起点：`64b0875e2d0ba1226e8a3ed5c3b2baf19807f5bc`
- 代码完成范围：架构、迁移、运行闭环、自动化测试、构建和本地发布证据。
- 用户验收范围：真实物理桌面任务和业务验收，由用户独立执行，不作为代码完成的替代证据。
- 外部发布范围：代码签名、负责人审批和公网发布凭据单独报告，不得用假凭据绕过。

## 完成规则

- `done`：实现、持久化、运行消费者和自动化验证均存在，且测试覆盖需求中的失败边界。
- `partial`：存在类型或生产者，但缺少运行消费者、恢复闭环或验收阈值。
- `open`：仍有明确代码工作。
- 不能用类型存在、搜索无结果、健康接口或模型叙述代替运行证据。

## 需求账本

| ID | 代码要求 | 起始状态 | 完成证据 |
| --- | --- | --- | --- |
| CAP-01 | `CapabilityExecutionPlan` 是聊天、语音、任务、定时/自主任务和智能体的唯一业务执行协议 | done | 五类入口均调用统一管线；27 个内置定时任务显式声明执行类别，在 handler 前按稳定时间槽生成、授权并持久化同一计划；执行中断视为结果未知且同槽禁止重放，本地通知使用稳定身份去重 |
| CAP-02 | 复杂能力计划生成真实节点边，固定 workflow 无语义决策权 | done | 计划包含选择、适配器、验证、汇合条件边；聊天/语音 workflow 共用无决策权适配器和统一回执 |
| CAP-03 | 旧 quick/workflow 快捷执行只读兼容且不能恢复外发 | done | `LEGACY_DIRECT_EXECUTION_ENABLED = false` 及回归测试 |
| DESK-01 | 应用身份注册、专用适配器、UIA、视觉末级降级 | done | 注册表联合校验进程、路径、签名、发布者、产品、版本和窗口类；原生端输出身份事实，执行顺序固定为专用适配器→UIA→视觉，外发禁用视觉降级 |
| DESK-02 | 前台变化、目标不匹配、未知外发、验证码和超时均停止 | done | 每次动作前后消费新窗口指纹；PID/句柄/窗口几何/DPI/显示器/应用身份变化令计划失效；条件认证应用不能外发，并暴露错配、陈旧观察、未知结果及认证阻断指标 |
| SELF-01 | 自我介绍完全由实时 `SelfModelSnapshot` 生成 | done | 身份、模型、能力、知识、运行状态及来源时间均来自实时快照；演示计划按快照动态生成 |
| SELF-02 | 聊天/语音共享快照，断连、未配置、异常和未认证能力实时限定 | done | 聊天/语音使用同一事实摘要和摘要哈希；缺失/过期状态产生刷新动作；注册表仅表达支持等级，本机应用在实时身份预检前明确标为未认证且无认证版本 |
| GRAPH-01 | 模型/内外智能体支持串行、并行、分解、投票、评审、反思、汇合和降级 | done | DAG 支持串行、并行、分解、聚合、首个验证、评审、多数投票、反思和候选降级；智能体节点继承同一权限、租约与回执 |
| GRAPH-02 | 检查模型可用性、上下文、隐私、成本、时间和用户指定模型 | done | 编译器检查候选可用性、上下文长度、本地隐私、输入/输出 token、费用和墙钟预算；用户锁定模型不可静默扩候选，副作用后禁止候选重放 |
| GRAPH-03 | 节点状态持久化，重启恢复且已完成副作用节点不重复执行 | done | 精确任务、图和节点语义指纹恢复已接入聊天、语音、任务和后台委派；原文不落库 |
| KNOW-01 | 索引与已验证吸收分离，清单记录版本、哈希、抽取、块、嵌入和引用 | done | 清单保存源版本/哈希、抽取、偏移/页码、块哈希、嵌入和引用；旧 `indexed` 每分钟限量后台复验，复验成功前保留历史向量和失败证据 |
| KNOW-02 | 抽取/分块/嵌入 100%，Recall@5 ≥ 95%，引用准确率 ≥ 98% | done | 固定 Top-5、95%/98% 门槛及精确边界、空语料和非 Top-5 拒绝测试 |
| KNOW-03 | 扫描件、图片、音频、损坏、加密、不支持格式不得伪装完整吸收 | done | 文本/Office/PDF/扫描件/图片/音频/损坏/加密/不支持格式矩阵测试；空抽取不写索引并持久化精确失败类别 |
| OBS-01 | 规划、模型、智能体、工具、桌面动作统一追踪并持久化摘要回执 | done | 追加式任务/回执账本保存脱敏计划、节点、幂等键和证据摘要；健康接口输出路由分歧、模型仲裁/降级/恢复、知识覆盖/召回/引用和桌面认证/错配指标 |
| SAFE-01 | 外部副作用确认绑定、目标不符/过期/未知即停止，禁止旧路由回退 | done | 确认、幂等、未知结果和影子分歧守卫已有回归 |
| TEST-01 | 聊天/语音双通道、故障注入、迁移/回滚、构建、安装包和 soak 自动门禁 | partial | 定时入口新增计划前置、持久回执、同槽去重和失败停止回归；修复提交后仍需重跑全量构建、安装冒烟、生命周期和两小时 soak |

## 用户侧物理验收交接

代码完成后另行生成物理验收表，至少覆盖 Lumi、浏览器、WPS/Office、微信、AutoCAD、桌面 AI，每类 40 个任务及计划规定的弹窗、遮挡、多显示器、DPI、崩溃、网络和重启场景。该表记录用户实测结果，不把健康接口轮询记作物理桌面任务。
